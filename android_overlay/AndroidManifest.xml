<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet: llamadas a la API de Gemini -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Micrófono: reconocimiento de voz -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Notificaciones (obligatorio pedirlo en runtime desde Android 13) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Alarmas exactas (el usuario debe habilitarlo en Ajustes en Android 12-13) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- Volver a programar las alarmas si el teléfono se reinicia -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Encender pantalla / mantener el CPU despierto al sonar la alarma -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Mostrar la pantalla de alarma encima del bloqueo -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:label="Asiste"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- flutter_local_notifications: necesarios para disparar y restaurar
             alarmas programadas, incluso con la app fuera de primer plano. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <!-- showWhenLocked + turnScreenOn son los que permiten que la pantalla
                 de alarma se abra encima del bloqueo y encienda la pantalla,
                 sin tocar código nativo. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Los receivers de flutter_local_notifications se declaran explícitamente
         arriba para que las alarmas programadas se disparen de forma fiable. -->

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
